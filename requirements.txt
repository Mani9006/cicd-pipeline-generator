# CI/CD Pipeline Generator - Python Dependencies
# Core dependencies (stdlib only - no external packages required)

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# Optional - for YAML validation
pyyaml>=6.0.0
